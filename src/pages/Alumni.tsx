// Alumni.tsx
// Placeholder for your Alumni Networking Platform project page.
// For now it just shows a title, later we can integrate real project or link to backend.

export default function Alumni() {
  return (
    <div style={{ textAlign: "center", marginTop: "50px" }}>
      <h1>Alumni Networking Platform</h1>
      <p>This will later be replaced with the real project details or app.</p>
    </div>
  );
}
